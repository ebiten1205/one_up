<main>
<% if @post.errors.any? %>
  <%= @post.errors.count %>件のエラーが発生しました
  <ul>
    <% @post.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
<div>
  <%= flash[:notice] %>
</div>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
    </div>
    <div class="col-md-8 offset-md-1">
      <h3>投稿一覧<h3>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>投稿内容</th>
            <th>実施場所</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @posts.each do |post| %>
          <tr>
            <td>
             <%= link_to customer_path(post.customer.id) do %>
             <%= image_tag post.customer.get_profile_image(80, 80) %>
             <% end %> 
            </td>
            <td>
             <%= link_to post.content, admin_post_path(post) %>
            </td>
            <td>
             <%= post.place %> 
            </td>
            <td>
              <i class="fas fa-heart" aria-hidden="true" style="color: red;"></i>
              <%= post.likes.count %>いいね
            </td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>
</main>

<% @post.post_comments.each do |post_comment| %>
  <%= post_comment.comment %>
<% end %>
  <%#= link_to "Destroy", post_post_comment_path(@posts, post_comment), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-danger pull-right" %>

<%# @posts.each do |post| %>
  <p><%#= link_to "#{post.post_comments.count} コメント", post_path(post.id) %></p>
<%# end %>