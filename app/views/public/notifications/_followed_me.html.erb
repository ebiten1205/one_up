<%= link_to transition_path(notification) do %>
  <div>
      <%= link_to notification.notificatable.follower.last_name, customer_path(notification.notificatable.follower) %>
    があなたをフォローしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>